---
title: TypeScript SDK
subtitle: Call the OpenMRP API from server-side TypeScript or JavaScript.
route: /api/typescript-sdk
nav:
    title: TypeScript
    section: API
    subsection: SDKs
    order: 1
---

The OpenMRP TypeScript SDK, `@openmrp/sdk`, is a typed client for the OpenMRP REST API, intended for server-side TypeScript and JavaScript. It ships request and response types for every endpoint, handles authentication, retries, and timeouts, and exposes the full API through a single `OpenMRP` client.

<DocCardGroup>
    <LinkCard
        pathKey="npm.typescriptSdk"
        title="npm package"
        description="@openmrp/sdk on the npm registry."
    />
    <LinkCard
        pathKey="github.typescriptSdk"
        title="Source on GitHub"
        description="open-mrp/typescript-sdk - source, changelog, and issues."
    />
</DocCardGroup>

## Installation

<DocTabs className="my-4">
<DocTab label="npm">

```sh
npm install @openmrp/sdk
```

</DocTab>
<DocTab label="pnpm">

```sh
pnpm add @openmrp/sdk
```

</DocTab>
<DocTab label="yarn">

```sh
yarn add @openmrp/sdk
```

</DocTab>
<DocTab label="bun">

```sh
bun add @openmrp/sdk
```

</DocTab>
</DocTabs>

## Initialize the client

Create a single `OpenMRP` client and reuse it across requests. Authenticate with an <InternalLink pathKey="api.apiKeys" text="API key" /> passed as `bearerToken`, and set `openMRPAccountID` to the account you want to operate on - it is sent as the `OpenMRP-Account` header on every request.

```ts
import OpenMRP from '@openmrp/sdk';

const client = new OpenMRP({
    bearerToken: process.env['OPENMRP_API_KEY'], // or omit - defaults to process.env['OPENMRP_API_KEY']
    openMRPAccountID: 'YOUR_SANDBOX_ACCOUNT_ID', // ID for the account YOUR_SANDBOX_ACCOUNT_NAME
});

const items = await client.catalog.items.list();

console.log(items.data);
```

> In production, load your key from an environment variable or secret manager rather than hardcoding it (`bearerToken` defaults to `process.env['OPENMRP_API_KEY']`, so you can omit it entirely). Treat the key like a password: never commit it or ship it to a browser. The SDK is intended for server-side use.

### Client options

| Option           | Type                                              | Default                                | Description                                                                  |
| :--------------- | :------------------------------------------------ | :------------------------------------- | :--------------------------------------------------------------------------- |
| `bearerToken`    | `string`                                          | `process.env['OPENMRP_API_KEY']`         | API key sent as a `Bearer` token.                                            |
| `openMRPAccountID` | `string`                                          | -                                      | Account UUID, sent as the `OpenMRP-Account` header. Update to switch accounts. |
| `environment`    | `'production'` \| `'local'`                       | `'production'`                         | Selects the base URL. `production` → `https://api.openmrp.ai`.                |
| `baseURL`        | `string`                                          | `process.env['OPENMRP_BASE_URL']`        | Override the base URL directly. Don't combine with `environment`.            |
| `maxRetries`     | `number`                                          | `2`                                    | Number of automatic retries for transient failures.                          |
| `timeout`        | `number`                                          | `60000`                                | Per-request timeout in milliseconds.                                         |
| `logLevel`       | `'debug' \| 'info' \| 'warn' \| 'error' \| 'off'` | `process.env['OPENMRP_LOG']` or `'warn'` | Controls log verbosity.                                                      |

## Switching accounts

If your integration spans multiple accounts, use `withOptions` to derive a client scoped to a different account without mutating the original. This is cheaper and safer than constructing a new client per account.

```ts
const otherAccount = client.withOptions({
    openMRPAccountID: 'YOUR_ACCOUNT_ID', // ID for the account YOUR_ACCOUNT_NAME
});

await otherAccount.catalog.items.list();
```

## Request & response types

Every request param and response field is typed. Import the types directly off the `OpenMRP` namespace, and rely on editor tooltips - each method, param, and field carries a docstring that appears on hover.

```ts
import OpenMRP from '@openmrp/sdk';

const client = new OpenMRP();

const items: OpenMRP.Catalog.ListItem = await client.catalog.items.list();
```

## Handling errors

When the SDK cannot reach the API, or the API returns a 4xx or 5xx response, it throws a subclass of `OpenMRP.APIError`. Inspect `status`, `name`, and `headers` to branch on the failure.

```ts
const items = await client.catalog.items.list().catch(async (err) => {
    if (err instanceof OpenMRP.APIError) {
        console.log(err.status); // 400
        console.log(err.name); // BadRequestError
        console.log(err.headers); // { server: 'nginx', ... }
    } else {
        throw err;
    }
});
```

Each status maps to a dedicated error class:

| Status code | Error class                |
| :---------- | :------------------------- |
| 400         | `BadRequestError`          |
| 401         | `AuthenticationError`      |
| 403         | `PermissionDeniedError`    |
| 404         | `NotFoundError`            |
| 409         | `ConflictError`            |
| 422         | `UnprocessableEntityError` |
| 429         | `RateLimitError`           |
| >= 500      | `InternalServerError`      |
| N/A         | `APIConnectionError`       |

For the structure of the error envelope and the full list of error codes, see <InternalLink pathKey="api.errors" text="API errors" />.

## Retries

The SDK automatically retries certain failures up to 2 times with a short exponential backoff. Connection errors, `408 Request Timeout`, `409 Conflict`, `429 Rate Limit`, and `>= 500` responses are retried by default. Configure or disable this per client or per request:

```ts
// For all requests:
const client = new OpenMRP({
    maxRetries: 0, // default is 2
});

// Or per request:
await client.catalog.items.list({
    maxRetries: 5,
});
```

## Timeouts

Requests time out after 1 minute by default and throw `APIConnectionTimeoutError` on expiry. Timed-out requests are still subject to the retry policy above.

```ts
// For all requests:
const client = new OpenMRP({
    timeout: 20 * 1000, // 20 seconds
});

// Or per request:
await client.catalog.items.list({
    timeout: 5 * 1000,
});
```

## Pagination

List endpoints return a `data` array alongside a `page_info` object with `has_next_page`, `has_prev_page`, and `next_page_url` / `previous_page_url`. The page URLs are relative paths that carry the next/previous `cursor` as a query parameter — pull that cursor out and pass it into the next `list` call. You can also set `limit` to control page size.

A small helper keeps the extraction in one place:

```ts
/**
 * Extracts the cursor query parameter from a pagination URL.
 * Pagination URLs are relative paths like "/v1/path?cursor=xxx&limit=10".
 */
export function extractCursorFromUrl(url: string | null | undefined): string | undefined {
    if (!url) return undefined;
    const qIdx = url.indexOf('?');
    if (qIdx < 0) return undefined;
    const params = new URLSearchParams(url.slice(qIdx + 1));
    return params.get('cursor') ?? undefined;
}
```

Then iterate until there are no more pages:

```ts
let page = await client.catalog.items.list({ limit: 100 });

for (;;) {
    for (const item of page.data) {
        console.log(item.id);
    }

    const cursor = extractCursorFromUrl(page.page_info.next_page_url);
    if (!page.page_info.has_next_page || !cursor) break;

    page = await client.catalog.items.list({ cursor });
}
```

See <InternalLink pathKey="api.pagination" text="Pagination" /> for the full cursor semantics.

## Accessing the raw response

Every method returns an `APIPromise`. Use `.asResponse()` to read headers without consuming the body, or `.withResponse()` to get the parsed data and the raw `Response` together.

```ts
const response = await client.catalog.items.list().asResponse();
console.log(response.headers.get('request-id'));

const { data, response: raw } = await client.catalog.items.list().withResponse();
console.log(raw.headers.get('request-id'));
console.log(data.data);
```

The `Request-ID` header is useful when reporting issues - see <InternalLink pathKey="api.requestIds" text="Request IDs" />.

## Logging

Set `logLevel` (or the `OPENMRP_LOG` environment variable) to control verbosity. At `debug`, all HTTP requests and responses are logged, including headers and bodies - use it only for local debugging.

```ts
const client = new OpenMRP({
    logLevel: 'debug', // 'debug' | 'info' | 'warn' (default) | 'error' | 'off'
});
```

By default the SDK logs to `globalThis.console`. You can supply a custom logger (pino, winston, bunyan, and most common loggers are supported) via the `logger` option.

## Versioning

The SDK is pinned to the API version it was generated against, so upgrading the package upgrades the API version your integration targets. Review the [changelog](https://github.com/open-mrp/typescript-sdk/blob/main/CHANGELOG.md) before bumping, and see <InternalLink pathKey="api.versioning" text="Versioning" /> for how OpenMRP versions the API.

## Requirements

TypeScript 4.9 or later is supported. The SDK runs on:

- Node.js 20 LTS or later (non-EOL versions)
- Deno v1.28.0 or later
- Bun 1.0 or later
- Cloudflare Workers and the Vercel Edge Runtime
- Modern browsers (server-side use is recommended so your API key stays secret)

React Native is not supported at this time.
