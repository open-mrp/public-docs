# name: Update Documentation

# on:
#   schedule:
#     # Run every 3 hours
#     - cron: "0 */3 * * *"
#   workflow_dispatch:
#     inputs:
#       spec_name:
#         description: "Name to use for the spec files"
#         required: true
#         default: "api"

# jobs:
#   update-docs:
#     runs-on: ubuntu-latest
#     timeout-minutes: 10
#     permissions:
#       id-token: write
#       contents: write
#       packages: read
#       pull-requests: write

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: "20"

#       - name: Setup Bun
#         uses: oven-sh/setup-bun@v2
#         with:
#           bun-version: latest

#       - name: Install dependencies
#         run: bun install
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

#       - name: Setup Go
#         uses: actions/setup-go@v5
#         with:
#           go-version: "1.21"

#       - name: Install go dependencies
#         run: go mod download

#       - name: Configure AWS Credentials
#         uses: aws-actions/configure-aws-credentials@v3
#         with:
#           role-to-assume: arn:aws:iam::013292575751:role/github-action-role
#           aws-region: us-east-2

#       - name: Download latest specs from S3
#         run: |
#           mkdir -p specs
#           SPEC_NAME=${{ github.event.inputs.spec_name || 'api' }}
#           aws s3 cp s3://augno-public-openapi/${SPEC_NAME}_public_spec.json specs/${SPEC_NAME}_public_spec.json

#       - name: Generate docs from OpenAPI spec
#         run: |
#           # Generate MDX files from OpenAPI spec
#           go run main.go
#           # Update static params for Next.js
#           bun run scripts/generate-slugs.ts

#       - name: Create Pull Request
#         run: |
#           git config --global user.email "actions@users.noreply.github.com"
#           git config --global user.name "docs-bot"
#           git add .

#           # Check if there are any changes
#           if git diff --quiet && git diff --staged --quiet; then
#             echo "No changes to commit"
#             exit 0
#           fi

#           # Create a new branch with timestamp
#           BRANCH_NAME="docs-update-$(date +%Y%m%d-%H%M%S)"
#           git checkout -b "$BRANCH_NAME"

#           # Commit changes
#           git commit -m "Automatic documentation update"

#           # Push the branch
#           git push origin "$BRANCH_NAME"

#           # Create pull request using GitHub CLI
#           gh pr create \
#             --title "Automatic documentation update" \
#             --body "This PR contains automatically generated documentation updates from the latest OpenAPI spec." \
#             --base main \
#             --head "$BRANCH_NAME"
#         env:
#           GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
